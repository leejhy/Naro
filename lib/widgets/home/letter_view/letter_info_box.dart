import 'package:flutter/material.dart';

class LetterInfoBox extends StatelessWidget {
  const LetterInfoBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}